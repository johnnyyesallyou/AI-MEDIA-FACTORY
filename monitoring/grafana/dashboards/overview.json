{
  "annotations": {"list": []},
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "id": null,
  "links": [],
  "liveNow": false,
  "panels": [
    {
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "fieldConfig": {
        "defaults": {"color": {"mode": "palette-classic"}},
        "overrides": []
      },
      "gridPos": {"h": 8, "w": 12, "x": 0, "y": 0},
      "id": 1,
      "options": {"legend": {"displayMode": "list"}},
      "targets": [
        {
          "expr": "rate(amf_jobs_total[5m])",
          "legendFormat": "{{job_name}} {{status}}"
        }
      ],
      "title": "Jobs per second",
      "type": "timeseries"
    },
    {
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "fieldConfig": {
        "defaults": {"color": {"mode": "palette-classic"}},
        "overrides": []
      },
      "gridPos": {"h": 8, "w": 12, "x": 12, "y": 0},
      "id": 2,
      "options": {"legend": {"displayMode": "list"}},
      "targets": [
        {
          "expr": "rate(amf_posts_published_total[5m])",
          "legendFormat": "{{platform}} {{channel}}"
        }
      ],
      "title": "Posts published per second",
      "type": "timeseries"
    },
    {
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "fieldConfig": {
        "defaults": {"color": {"mode": "palette-classic"}},
        "overrides": []
      },
      "gridPos": {"h": 8, "w": 12, "x": 0, "y": 8},
      "id": 3,
      "options": {"legend": {"displayMode": "list"}},
      "targets": [
        {
          "expr": "histogram_quantile(0.95, rate(amf_job_duration_seconds_bucket[5m]))",
          "legendFormat": "{{job_name}} p95"
        }
      ],
      "title": "Job duration p95",
      "type": "timeseries"
    },
    {
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "fieldConfig": {"defaults": {}, "overrides": []},
      "gridPos": {"h": 8, "w": 12, "x": 12, "y": 8},
      "id": 4,
      "options": {},
      "targets": [
        {
          "expr": "amf_channels_active",
          "legendFormat": "Active channels"
        },
        {
          "expr": "amf_posts_in_queue",
          "legendFormat": "Posts in queue"
        }
      ],
      "title": "System state",
      "type": "stat"
    }
  ],
  "schemaVersion": 38,
  "style": "dark",
  "tags": ["amf"],
  "templating": {"list": []},
  "time": {"from": "now-1h", "to": "now"},
  "title": "AI Media Factory Overview",
  "uid": "amf-overview"
}